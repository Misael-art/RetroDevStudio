# Tauri build configuration
[build]
# Keep the alternate target directory inside src-tauri so root tree checks stay green.
# Hosts blocked by AppLocker can still override this with CARGO_TARGET_DIR (for example on C:).
target-dir = "target-test"
jobs = 2

[env]
RUST_MIN_STACK = "16777216"
